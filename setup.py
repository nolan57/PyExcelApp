from setuptools import setup, find_packages

setup(
    name="pyexcelapp",
    version="0.1",
    package_dir={"": ""},
    packages=find_packages(where=""),
    install_requires=[
        # 项目依赖
    ]
)

[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 添加所有需要的标记
markers =
    monitoring: dependency monitoring tests
    plugin: plugin system tests
    excel: excel operation tests
    response: response tests
    utils: utility tests
    integration: integration tests
    performance: performance tests
    functional: functional tests
    benchmark: benchmark tests
    slow: slow running tests
    gui: GUI tests

# 添加路径配置
pythonpath = 
    .
    dependency_monitoring_framework
    plugin_manager
    src

# 移除特定标记的限制
# addopts = -v -m "monitoring" 